# FT-02 v2.75e Communication UX A1

## 1. 已投递消息撤销
发件箱选择状态为“已送达”的消息，按 DEL。
预期提示：
`消息已经投递，无法撤销`

## 2. 通讯模块文案
节点/私信相关页面不再显示 `LR01：在线`。
预期显示：
`通讯模块：在线`

## 3. 清空发件箱
发件箱按 `C`。
先进入确认页，不应立即清空。
- ENTER/确认键：确认清空
- B / ESC：取消

确认后提示：
`发件箱已清空`
